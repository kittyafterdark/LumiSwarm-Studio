diff --git a/src/spindle/worker-host.ts b/src/spindle/worker-host.ts
index 6e9ffa9..9660546 100644
--- a/src/spindle/worker-host.ts
+++ b/src/spindle/worker-host.ts
@@ -2951,6 +2951,13 @@ export class WorkerHost {
     }
     this.enforceScopedUser(resolvedUserId);
 
+    const jobId = typeof input?.clientJobId === "string" && input.clientJobId.trim()
+      ? input.clientJobId.trim()
+      : crypto.randomUUID();
+    const ownerChatId = typeof input?.owner_chat_id === "string" && input.owner_chat_id.trim()
+      ? input.owner_chat_id.trim()
+      : undefined;
+
     try {
       // Resolve connection
       const connectionId = input.connection_id || null;
@@ -2971,12 +2978,54 @@ export class WorkerHost {
       // Merge connection defaults with request parameters
       const mergedParams = { ...connection.default_parameters, ...(input.parameters || {}) };
 
-      const response = await provider.generate(apiKey || "", connection.api_url || "", {
+      const generationRequest = {
         prompt: input.prompt || "",
         negativePrompt: input.negativePrompt,
         model: input.model || connection.model,
         parameters: mergedParams,
-      });
+      };
+
+      let response: Awaited<ReturnType<typeof provider.generate>>;
+      const stream = provider.generateStream;
+      if (typeof stream === "function") {
+        const iterator = stream.call(
+          provider,
+          apiKey || "",
+          connection.api_url || "",
+          generationRequest,
+        );
+        let lastStep = 0;
+        let lastTotalSteps = 0;
+        while (true) {
+          const next = await iterator.next();
+          if (next.done) {
+            response = next.value;
+            break;
+          }
+          const chunk = next.value;
+          if (typeof chunk.step === "number") lastStep = chunk.step;
+          if (typeof chunk.totalSteps === "number") lastTotalSteps = chunk.totalSteps;
+          eventBus.emit(
+            EventType.IMAGE_GEN_PROGRESS,
+            {
+              assetId: jobId,
+              chatId: ownerChatId,
+              extensionIdentifier: this.manifest.identifier,
+              step: typeof chunk.step === "number" ? chunk.step : lastStep,
+              totalSteps: typeof chunk.totalSteps === "number" ? chunk.totalSteps : lastTotalSteps,
+              preview: chunk.preview,
+              nodeId: chunk.nodeId,
+            },
+            resolvedUserId,
+          );
+        }
+      } else {
+        response = await provider.generate(
+          apiKey || "",
+          connection.api_url || "",
+          generationRequest,
+        );
+      }
 
       // Persist image to the images table
       let imageId: string | undefined;
@@ -2993,9 +3042,7 @@ export class WorkerHost {
               owner_character_id: typeof input?.owner_character_id === "string" && input.owner_character_id.trim()
                 ? input.owner_character_id.trim()
                 : undefined,
-              owner_chat_id: typeof input?.owner_chat_id === "string" && input.owner_chat_id.trim()
-                ? input.owner_chat_id.trim()
-                : undefined,
+              owner_chat_id: ownerChatId,
             }
           );
           imageId = image.id;
@@ -3005,12 +3052,34 @@ export class WorkerHost {
         }
       }
 
+      eventBus.emit(
+        EventType.IMAGE_GEN_COMPLETE,
+        {
+          assetId: jobId,
+          chatId: ownerChatId,
+          extensionIdentifier: this.manifest.identifier,
+          imageId,
+          imageUrl,
+        },
+        resolvedUserId,
+      );
+
       this.postToWorker({
         type: "response",
         requestId,
-        result: { ...response, imageId, imageUrl },
+        result: { ...response, imageId, imageUrl, jobId },
       });
     } catch (err: any) {
+      eventBus.emit(
+        EventType.IMAGE_GEN_ERROR,
+        {
+          assetId: jobId,
+          chatId: ownerChatId,
+          extensionIdentifier: this.manifest.identifier,
+          message: err?.message ?? String(err),
+        },
+        resolvedUserId,
+      );
       this.postToWorker({ type: "response", requestId, error: err.message });
     }
   }
diff --git a/developer-docs/docs/backend-api/image-generation.md b/developer-docs/docs/backend-api/image-generation.md
index 2fc6738..f14ca79 100644
--- a/developer-docs/docs/backend-api/image-generation.md
+++ b/developer-docs/docs/backend-api/image-generation.md
@@ -12,10 +12,36 @@ Generate an image using a connection profile. If `connection_id` is omitted, use
 const result = await spindle.imageGen.generate({
   prompt: 'A serene mountain landscape at sunset, anime style',
   connection_id: 'optional-connection-id',
+  clientJobId: crypto.randomUUID(),
 })
-// result: { imageDataUrl: "data:image/png;base64,...", model: "...", provider: "..." }
+// result: { imageDataUrl: "data:image/png;base64,...", model: "...", provider: "...", jobId: "..." }
 ```
 
+When `clientJobId` is supplied, frontend code can subscribe to the same
+job-scoped live preview events used by Lumiverse's built-in image panel:
+
+```ts
+const jobId = crypto.randomUUID()
+
+const unsubscribe = ctx.events.on('IMAGE_GEN_PROGRESS', (progress) => {
+  if (progress.assetId !== jobId) return
+  previewImage.src = progress.preview
+  progressLabel.textContent = `${progress.step} / ${progress.totalSteps}`
+})
+
+await spindle.imageGen.generate({
+  prompt: 'A serene mountain landscape at sunset, anime style',
+  connection_id: 'optional-connection-id',
+  clientJobId: jobId,
+})
+
+unsubscribe()
+```
+
+Providers with a streaming implementation, currently ComfyUI and SwarmUI,
+emit `IMAGE_GEN_PROGRESS` frames. All providers emit `IMAGE_GEN_COMPLETE` or
+`IMAGE_GEN_ERROR` with the same `assetId`.
+
 You can override the model and pass provider-specific parameters:
 
 ```ts
@@ -58,6 +84,7 @@ const result = await spindle.imageGen.generate({
 | `parameters` | `Record<string, unknown>` | Optional. Provider-specific parameters, merged with the connection's `default_parameters`. |
 | `owner_character_id` | `string` | Optional. Tag the persisted generated image to a specific character. |
 | `owner_chat_id` | `string` | Optional. Tag the persisted generated image to a specific chat. |
+| `clientJobId` | `string` | Optional correlation ID for `IMAGE_GEN_PROGRESS`, `IMAGE_GEN_COMPLETE`, and `IMAGE_GEN_ERROR` WebSocket events. A UUID is generated when omitted. |
 | `userId` | `string` | **Required for operator-scoped extensions.** |
 
 ### ImageGenResultDTO
@@ -69,7 +96,8 @@ const result = await spindle.imageGen.generate({
 | `provider` | `string` | The provider that was used (`google_gemini`, `nanogpt`, `novelai`). |
 | `imageId` | `string` | Optional. The persisted image ID in the images table. Present when persistence succeeds. |
 | `imageUrl` | `string` | Optional. Public URL path for the image (e.g. `/api/v1/image-gen/results/{id}`). Works without authentication — suitable for push notification `image` field, embeds, and external references. |
+| `jobId` | `string` | Correlation ID used for live progress events. Equals `clientJobId` when supplied. |
 
 ### Image Persistence
 
 Generated images are automatically saved to the images table with full thumbnail support. The persisted row is automatically tagged to the current extension, and you can additionally pass `owner_character_id` and/or `owner_chat_id` to make later retrieval via `spindle.images.list()` and `spindle.images.get()` much cheaper and more targeted.
